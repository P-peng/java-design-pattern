代理模式

情况假设：jdk动态代理

主要角色
    1.  真实主题类
    2.  真实主题类 抽象主题
    3.  代理类
    4.  代理类管理者（jdk动态代理）

优点：
    1.  代理模式在客户端与目标对象之间起到一个中介作用和保护目标对象的作用；其最强大就是中介左右，可在中介做方法增强

缺点：
    1.  增加了系统的复杂度
    2.  在客户端和目标对象之间增加一个代理对象，会造成请求处理速度变慢；（jdk动态代理会如次，但是cglib不会，）


应用场景：
    1.  spring中，jdk动态代理，cglib动态代理

